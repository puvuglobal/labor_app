-- Direct SQL to create test users
-- Run this in Supabase SQL Editor

-- Test users will be created via auth, but we'll bypass the trigger temporarily
-- First, let's check if we can create users

-- If auth is failing, let's try creating them through the auth system directly
-- But since we can't do that easily, let's try a different approach

-- Actually, let's just try to insert a simple test to see what's happening
SELECT 'Testing connection' as status;
